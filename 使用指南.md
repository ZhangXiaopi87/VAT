# 文件使用指南

本文档说明 `20251224` 文件夹中各个文件的使用方法和执行顺序。

## 文件结构

```
20251224/
├── pbmc5k_data_celltype.py          # 数据加载器（生成细胞类型标签）
├── base/                             # 模型架构（256-128-64）
│   ├── __init__.py
│   ├── autoencoder.py
│   ├── self_supervised_vime.py
│   └── self_supervised_multilayer.py
├── VAT/
│   └── pbmc5k_vat_celltype.py        # VAT 方法运行脚本
├── VIME/
│   └── pbmc5k_vime_celltype.py       # VIME 方法运行脚本
├── VAT_VIME_Feature/
│   └── pbmc5k_vat_vime_feature_celltype.py  # VAT_VIME_Feature 方法运行脚本
├── run_all_celltype.sh               # 批量运行所有方法
├── generate_results_summary.py       # 生成结果汇总
└── README_CELLTYPE.md                # 详细说明文档
```

## 使用顺序

### 步骤 1：测试数据加载器（可选）

验证数据加载是否正常：

```bash
cd /userhome/home/zhangqirui/project/self-play/tem/20251224
python3 pbmc5k_data_celltype.py
```

**作用**：
- 加载 PBMC 5k 数据集
- 基于蛋白质标记生成细胞类型标签（6类）
- 分割数据集为 labeled/unlabeled/validation/test

**输出**：控制台显示数据加载信息和细胞类型分布

---

### 步骤 2：运行实验

有两种方式运行实验：

#### 方式 A：批量运行（推荐）

一键运行所有三个方法：

```bash
cd /userhome/home/zhangqirui/project/self-play/tem/20251224
chmod +x run_all_celltype.sh
./run_all_celltype.sh
```

**执行顺序**：
1. VAT 方法
2. VAT_VIME_Feature 方法
3. VIME 方法

**结果保存位置**：
- `VAT/results_celltype/`
- `VAT_VIME_Feature/results_celltype/`
- `VIME/results_celltype/`

#### 方式 B：单独运行

如果需要单独运行某个方法：

```bash
# 运行 VAT
cd /userhome/home/zhangqirui/project/self-play/tem/20251224/VAT
python3 pbmc5k_vat_celltype.py

# 运行 VIME
cd /userhome/home/zhangqirui/project/self-play/tem/20251224/VIME
python3 pbmc5k_vime_celltype.py

# 运行 VAT_VIME_Feature
cd /userhome/home/zhangqirui/project/self-play/tem/20251224/VAT_VIME_Feature
python3 pbmc5k_vat_vime_feature_celltype.py
```

---

### 步骤 3：生成结果汇总

在所有实验运行完成后，生成汇总报告：

```bash
cd /userhome/home/zhangqirui/project/self-play/tem/20251224
python3 generate_results_summary.py
```

**作用**：
- 读取三个方法的结果 JSON 文件
- 生成对比表格（均值、标准差、最小值、最大值）
- 保存为 Markdown 格式

**输出**：`results_summary_celltype.md`

---

## 文件说明

### 核心文件

| 文件 | 作用 | 是否必须运行 |
|------|------|------------|
| `pbmc5k_data_celltype.py` | 数据加载和标签生成 | 否（由各方法脚本自动调用） |
| `run_all_celltype.sh` | 批量运行脚本 | 是（或单独运行各方法） |
| `generate_results_summary.py` | 结果汇总生成 | 否（可选，用于查看对比结果） |

### 模型架构（base/）

这些文件定义了模型架构，由各方法脚本自动导入使用，**无需手动运行**：
- `autoencoder.py` - Autoencoder 模型
- `self_supervised_vime.py` - VIME-Self 模型
- `self_supervised_multilayer.py` - 多层 VIME 模型

### 方法脚本

| 脚本 | 方法 | 输出目录 |
|------|------|---------|
| `VAT/pbmc5k_vat_celltype.py` | VAT 半监督学习 | `VAT/results_celltype/` |
| `VIME/pbmc5k_vime_celltype.py` | VIME 半监督学习 | `VIME/results_celltype/` |
| `VAT_VIME_Feature/pbmc5k_vat_vime_feature_celltype.py` | VAT + VIME 特征 | `VAT_VIME_Feature/results_celltype/` |

---

## 快速开始

最简单的使用流程：

```bash
# 1. 进入目录
cd /userhome/home/zhangqirui/project/self-play/tem/20251224

# 2. 运行所有实验
./run_all_celltype.sh

# 3. 生成结果汇总（可选）
python3 generate_results_summary.py
```

---

## 注意事项

1. **环境要求**：需要安装 `torch`, `numpy`, `h5py`, `pandas` 等依赖
2. **GPU**：脚本默认使用 `CUDA_VISIBLE_DEVICES=1`，可根据实际情况修改
3. **数据路径**：数据文件路径在 `pbmc5k_data_celltype.py` 中定义，确保路径正确
4. **运行时间**：每个方法约需 15-40 分钟（取决于硬件配置）

---

## 更多信息

详细的技术说明、架构设计和实验配置请参考 `README_CELLTYPE.md`。

